<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sun City Bus Sales - Quality Buses in El Paso, Texas</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- jQuery -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <?=$js?>
    
    <style>
        :root {
            --primary-blue: #3b4cb8;
            --accent-orange: #f39c12;
            --dark-gray: #2c3e50;
            --light-gray: #f8f9fa;
            --medium-gray: #6c757d;
            --border-light: #e9ecef;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            color: var(--dark-gray);
            font-weight: 400;
        }
        
        .top-header {
            background: linear-gradient(135deg, var(--dark-gray) 0%, var(--primary-blue) 100%);
            color: white;
            padding: 10px 0;
            font-size: 13px;
            font-weight: 500;
        }
        
        .top-header a:hover {
            color: var(--accent-orange) !important;
            transition: color 0.3s ease;
        }
        
        .main-header {
            background: white;
            box-shadow: 0 2px 20px rgba(59, 76, 184, 0.08);
            border-bottom: 1px solid var(--border-light);
        }
        
        .logo-img {
            height: 90px;
            width: auto;
        }
        
        .company-title {
            color: var(--primary-blue);
            font-weight: 700;
            letter-spacing: -0.5px;
        }
        
        .navbar-nav .nav-link {
            font-weight: 500;
            padding: 18px 24px !important;
            color: var(--dark-gray) !important;
            position: relative;
            transition: all 0.3s ease;
        }
        
        .navbar-nav .nav-link:hover {
            color: var(--primary-blue) !important;
        }
        
        .navbar-nav .nav-link.active {
            color: var(--primary-blue) !important;
            background: linear-gradient(135deg, rgba(59, 76, 184, 0.05) 0%, rgba(243, 156, 18, 0.05) 100%);
        }
        
        .navbar-nav .nav-link.active::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 30px;
            height: 3px;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--accent-orange) 100%);
            border-radius: 2px;
        }
    </style>
</head>
<body>
    <!-- Top Header -->
    <div class="top-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <div class="d-flex gap-4 flex-wrap">
                        <a href="tel:915-613-9899" class="text-white text-decoration-none d-flex align-items-center">
                            <i class="fas fa-phone me-2"></i>(915) 613-9899
                        </a>
                        <a href="mailto:info@SunCityBusSales.com" class="text-white text-decoration-none d-flex align-items-center">
                            <i class="fas fa-envelope me-2"></i>info@SunCityBusSales.com
                        </a>
                    </div>
                </div>
                <div class="col-md-6 text-md-end mt-2 mt-md-0">
                    <span class="d-flex align-items-center justify-content-md-end">
                        <i class="fas fa-map-marker-alt me-2"></i>8015 Alameda, El Paso TX 79915
                    </span>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Header -->
    <header class="main-header sticky-top">
        <div class="container">
            <nav class="navbar navbar-expand-lg navbar-light py-3">
                <!-- Logo -->
                <div class="d-flex align-items-center">
                    <img src="/images/logo.png" alt="Sun City Bus Sales Logo" class="logo-img me-3">
                </div>

                <!-- Mobile Toggle -->
                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Navigation Menu -->
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link active" href="/home">
                                <i class="fas fa-home me-2"></i>Home
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/contact">
                                <i class="fas fa-phone me-2"></i>Contact
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    </header>