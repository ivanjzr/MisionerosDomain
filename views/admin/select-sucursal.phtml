<?php

//
include PATH_BASE.DS.'js_versioning.phtml';
//
$user_info = $App::GetUserData();
//var_dump($user_info); exit;

//
$App::$header_scrits = <<<EOF

EOF;

//
$App::$footer_scripts = <<<EOF
    <script src="/adm/plugins/jquery-form/jquery.form.min.js"></script>
    <script src="/adm/plugins/jquery-validation/jquery.validate.js"></script>    
EOF;

//
$App::$footer_section_scripts = <<<EOF
    <script src="/app/select-sucursal.js?v=$js_files_version"></script>
EOF;

//
$App::setSectionInfo("Dashboard", "dashboard");
//
include PATH_VIEWS_ADMIN.DS.'common'.DS.'header-login.phtml';
?>

<div class="min-vh-100 d-flex align-items-center justify-content-center bg-light">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                
                <!-- Logo Section -->
                <div class="text-center mb-4">
                    <?php
                    $str_size = "width:215px;margin-bottom:20px;";
                    if ( $user_info['account_id'] === 6 ){
                        $str_size = "width:400px;margin-bottom:40px;";
                    }
                    ?>
                    <img src="/logos<?=$user_info['login_logo_path'];?>" 
                         alt="<?=$user_info['app_name'];?>" 
                         style="<?=$str_size;?>"
                         class="img-fluid">
                </div>

                <!-- Main Card -->
                <div class="card border-0 shadow-sm">
                    
                    <!-- Header -->
                    <div class="card-header bg-white border-bottom-0 py-4">
                        <div class="text-center">
                            <h4 class="mb-2 fw-bold text-dark">Seleccionar Sucursal</h4>
                            <p class="text-muted mb-0">Elige la sucursal donde trabajarás hoy</p>
                        </div>
                    </div>

                    <!-- Body -->
                    <div class="card-body px-4 pb-4">
                        
                        <!-- User Info -->
                        <div class="card border-start border-primary border-4 mb-4">
                            <div class="card-body py-3">
                                <div class="row align-items-center">
                                    <div class="col-auto">
                                        <i class="fas fa-user-circle fa-2x text-primary"></i>
                                    </div>
                                    <div class="col">
                                        <h6 class="mb-1 text-primary fw-bold">
                                            <?= $user_info['nombre'];?>
                                            <?=($user_info['is_admin'] ? '<span class="badge bg-primary ms-2">Admin</span>':"");?>
                                        </h6>
                                        <p class="mb-0 text-muted">Sucursales asignadas disponibles</p>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Form -->
                        <form method="post" 
                              enctype="multipart/form-data" 
                              id="form_select_sucursal" 
                              name="form_select_sucursal">
                            
                            <!-- Store Selection -->
                            <div class="mb-4">
                                <label for="sucursal_id" class="form-label fw-medium text-dark">
                                    <i class="fas fa-store me-2"></i>
                                    Sucursal:
                                </label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light border-end-0">
                                        <i class="fas fa-building text-muted"></i>
                                    </span>
                                    <select data-rule-required="true" 
                                            data-rule-number="true" 
                                            class="form-select border-start-0" 
                                            id="sucursal_id" 
                                            name="sucursal_id" 
                                            disabled>
                                        <option value="">-- Seleccionar sucursal --</option>
                                    </select>
                                </div>
                            </div>

                            <!-- Submit Button -->
                            <div class="d-grid mb-3">
                                <button type="submit" class="btn btn-primary btn-lg py-3">
                                    <i class="fas fa-arrow-right me-2"></i>
                                    Continuar
                                </button>
                            </div>
                        </form>

                        <!-- Logout Link -->
                        <div class="text-center pt-3 border-top">
                            <a class="btnLogout text-decoration-none text-muted" 
                               data-type="admin" 
                               href="#!">
                                <i class="fas fa-sign-out-alt me-2"></i>
                                Cerrar Sesión
                            </a>
                        </div>

                    </div>
                </div>

                <!-- Footer Info -->
                <div class="text-center mt-4">
                    <small class="text-muted">
                        Selecciona tu sucursal para acceder al sistema
                    </small>
                </div>

            </div>
        </div>
    </div>
</div>

<?php
include PATH_VIEWS_ADMIN.DS.'common'.DS.'footer-login.phtml';
?>